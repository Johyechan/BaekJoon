#include <iostream>

using namespace std;

// 실버 5 (실패)_ 그리디 알고리즘 이해하고 풀기

int main()
{
	int N, M;
	cin >> N >> M;

	for (int i = 0; i < N; i++)
	{
		int x, y;
		cin >> x >> y;
	}
	for (int j = 0; j < M; j++)
	{
		int x, y;
		char c;
		cin >> x >> y >> c;
	}

	if (M % 2 != 0)
		cout << "jhnah917";
	else
		cout << "jhnan917";
}